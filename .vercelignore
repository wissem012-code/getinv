# Ignore files that shouldn't be deployed to Vercel

# Dependencies
node_modules

# Development files
.env
.env.*
!.env.example

# Build artifacts (will be rebuilt)
# Exclude build/client from root (deployed via outputDirectory)
# BUT DO NOT exclude build/server - needed by api/index.js serverless function
# Root api/ directory is NOT excluded - needed for serverless function detection
build/client
# Note: build/server is needed by root api/index.js, so we keep it in deployment
.react-router

# Shopify development files
.shopify
.shopify.lock

# IDE files
.vscode
.idea
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage
.nyc_output

# Prisma dev database
prisma/dev.sqlite
prisma/dev.sqlite-journal

# Extensions (handled separately)
extensions/*/dist
