# Ignore files that shouldn't be deployed to Vercel

# Dependencies
node_modules

# Development files
.env
.env.*
!.env.example

# Build artifacts (will be rebuilt)
# Note: build/client and build/server are both needed for deployment
# build/client - static assets (React Router client build)
# build/server - server build needed by api/index.js serverless function
# Only exclude build subdirectories that aren't needed
.react-router

# Shopify development files
.shopify
.shopify.lock

# IDE files
.vscode
.idea
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage
.nyc_output

# Prisma dev database
prisma/dev.sqlite
prisma/dev.sqlite-journal

# Extensions (handled separately)
extensions/*/dist
